meta {
  name: Health Check - Identity Service
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}api/auth/health
  body: none
  auth: none
}

docs {
  # Health Check - Identity Service
  - Direct health endpoint routed through the gateway.
  - Should return `{ "status": "healthy" }`.
}

meta {
  name: Get Student By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}{{v1Path}}/students/{{studentId}}?expand=&fields=
  body: none
  auth: bearer
}

params:query {
  expand: 
  fields: 
}

params:path {
  studentId: {{studentId}}
}

headers {
  Accept: application/json
  X-Request-Id: {{xRequestId}}
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  studentId: 1
}

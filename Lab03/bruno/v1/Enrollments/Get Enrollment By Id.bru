meta {
  name: Get Enrollment By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}api/enrollments/{{enrollmentId}}?expand=&fields=
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  expand: 
  fields: 
}

params:path {
  enrollmentId: {{enrollmentId}}
}

headers {
  Accept: application/json
}

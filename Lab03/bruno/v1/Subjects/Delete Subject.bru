meta {
  name: Delete Subject
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}api/subjects/{{subjectId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  subjectId: {{subjectId}}
}

headers {
  Accept: application/json
}
